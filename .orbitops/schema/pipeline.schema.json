{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://orbitops.dev/schema/pipeline.schema.json",
  "title": "Salesforce DevOps pipeline configuration",
  "type": "object",
  "additionalProperties": false,
  "required": ["pipeline"],
  "properties": {
    "workItems": {
      "type": "object",
      "additionalProperties": false,
      "description": "Work-item traceability policy. Omitted or false keeps Jira/Azure DevOps tagging optional.",
      "properties": {
        "required": {
          "type": "boolean",
          "default": false,
          "description": "Require every promotion PR to carry a Jira or Azure DevOps work-item reference"
        }
      }
    },
    "devOrgs": {
      "type": "array",
      "description": "Orgs builders can pull changes from (any sandbox/scratch/dev org)",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "org", "authMethod"],
        "properties": {
          "name": { "type": "string", "minLength": 1, "description": "Display name shown to builders" },
          "org": {
            "type": "string",
            "pattern": "^[A-Z][A-Z0-9_]*$",
            "description": "Org key — prefixes the repo-level auth secrets (<KEY>_SF_AUTH_URL / <KEY>_SF_CLIENT_ID…)"
          },
          "authMethod": { "enum": ["jwt", "sfdx-url"] }
        }
      }
    },
    "pipeline": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["branch", "org", "environment", "authMethod", "gates"],
        "properties": {
          "authMethod": {
            "enum": ["jwt", "sfdx-url"],
            "description": "How CI authenticates to this stage's org (see .github/actions/sf-auth)"
          },
          "testLevel": {
            "enum": ["NoTestRun", "RunLocalTests", "RunRelevantTests", "Conditional"],
            "description": "Apex test level for validation/deploy; Conditional = NoTestRun unless the delta contains Apex",
            "default": "RunLocalTests"
          },
          "quickDeploy": {
            "type": "boolean",
            "description": "Use a matching successful Salesforce validation for Quick Deploy. Set false to always run a normal deployment.",
            "default": true
          },
          "policy": {
            "type": "object",
            "additionalProperties": false,
            "required": ["owner"],
            "properties": {
              "owner": {
                "enum": ["developer", "central"],
                "description": "Developer stages accept per-request execution choices; central stages enforce their shared stage policy"
              }
            }
          },
          "branch": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$",
            "description": "Long-lived Git branch mapped to this stage"
          },
          "org": {
            "type": "string",
            "pattern": "^[A-Z][A-Z0-9_]*$",
            "description": "Logical org key (matches the secret set / CLI alias, e.g. INT, UAT, PROD)"
          },
          "environment": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$",
            "description": "GitHub Environment name guarding deploys to this stage"
          },
          "gates": {
            "type": "object",
            "additionalProperties": false,
            "required": ["scannerMaxSeverity", "minCoverage"],
            "properties": {
              "scannerMaxSeverity": {
                "type": "integer",
                "minimum": 1,
                "maximum": 5,
                "description": "Scanner findings at this severity or more severe (lower number) block promotion"
              },
              "minCoverage": {
                "type": "integer",
                "minimum": 0,
                "maximum": 100,
                "description": "Minimum Apex test coverage percentage required to promote"
              },
              "requiredReviewers": {
                "type": "boolean",
                "description": "Whether the GitHub Environment enforces required reviewers (informational; enforced in GitHub settings)"
              },
              "workItemStatus": {
                "type": "array",
                "items": { "type": "string", "minLength": 1 },
                "minItems": 1,
                "description": "Post-PoC: allowed tracker statuses for attached work items"
              }
            }
          }
        }
      }
    }
  }
}
