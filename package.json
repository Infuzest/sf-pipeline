{
  "name": "sf-pipeline",
  "private": true,
  "type": "module",
  "description": "Private OrbitOps reusable workflows, actions, and pipeline runtime",
  "scripts": {
    "config:validate": "node scripts/validate-pipeline-config.mjs scripts/__fixtures__/pipeline.yml",
    "test": "node --test \"scripts/**/*.test.mjs\""
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "js-yaml": "^4.1.0"
  }
}
